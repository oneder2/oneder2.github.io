# Tests

Run the generated-output tests after building:

```bash
npm run build
npm test
```

The suite checks the versioned API, localized outputs, legacy project compatibility, resolved LaTeX templates, and the website API path.
